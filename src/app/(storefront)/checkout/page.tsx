import { CheckoutView } from '@/features/storefront/components/checkout/checkout-view';

export const metadata = {
  title: 'Checkout | Sukoon'
};

export default function CheckoutPage() {
  return <CheckoutView />;
}
